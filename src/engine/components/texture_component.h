#if !defined(TEXTURE_COMPONENT_H)

#include "../core/core.h"

COMPONENT(
          // Name of Component
          Texture_Component,

          // Start of Component Data
          uint32_t texture_id;
         )

#define TEXTURE_COMPONENT_H
#endif
